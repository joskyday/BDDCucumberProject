package com.zerobank.stepdefinitions;

import com.zerobank.utilities.BrowserUtils;
import com.zerobank.utilities.ConfigurationReader;
import com.zerobank.utilities.Driver;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;

public class Manueldeneme {
  /*  public static void main(String[] args) {
        Driver.get().get(ConfigurationReader.get("url"));
        Driver.get().findElement(By.cssSelector("i.icon-signin")).click();
        BrowserUtils.waitFor(3);
        Driver.get().findElement(By.cssSelector("input[id='user_login']")).sendKeys(ConfigurationReader.get("username"));
        Driver.get().findElement(By.cssSelector("input[id='user_password']")).sendKeys(ConfigurationReader.get("password")+ Keys.ENTER);
        Driver.get().findElement(By.cssSelector("button#details-button")).click();
        Driver.get().findElement(By.cssSelector("a#proceed-link")).click();
    }*/
}
